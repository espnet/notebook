# Courses/CMUSpeechTechnology26S/speaker_verification.ipynb, every Sunday. The badge on this workflow is what the
# README shows beside that notebook, which is why it has a file of its own:
# GitHub's badge is per workflow, and cannot show one job of a matrix.

name: speaker_verification

on:
  schedule:
    # Sundays, 05:00 UTC
    - cron: "0 5 * * 0"
  workflow_dispatch:
  pull_request:
    paths:
      - "Courses/CMUSpeechTechnology26S/speaker_verification.ipynb"
      - "tools/**"
      - ".github/workflows/speaker_verification.yml"
      - ".github/workflows/_run_notebook.yml"

permissions:
  contents: read

jobs:
  run:
    uses: ./.github/workflows/_run_notebook.yml
    with:
      notebook: Courses/CMUSpeechTechnology26S/speaker_verification.ipynb
